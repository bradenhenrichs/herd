{
  "name": "herd-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local, read-only MCP server exposing your own Herdwatch herd to an MCP client (Claude Desktop, ChatGPT, …).",
  "bin": { "herd-mcp": "./herd-mcp.mjs" },
  "scripts": {
    "start": "node herd-mcp.mjs",
    "smoke": "node smoke-test.mjs"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  }
}
